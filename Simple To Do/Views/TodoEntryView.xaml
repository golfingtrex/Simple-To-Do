<UserControl x:Class="Simple_To_Do.Views.TodoEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Simple_To_Do.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Grid>

            <!--Grid Column formatting. Needs work to get it correctly centered-->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!--Button for adding Todo Item to Todo List-->
            <Button
                    Grid.Column="0"
                    Name="addTodoButton"
                    Content="Add Todo"
                    Width="100"
                    Height="30"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0 0 10 0"/>

            <!--Text Box for typing in the Todo Item-->
            <TextBox
                    Grid.Column="1"
                    Name="todoInputTextBox"/>
        </Grid>
    </StackPanel>
</UserControl>
